downloads:
  image: debian
  volumes:
    -  "/mnt/storage/volumes/downloads:/downloads"
  command: true

transmission:
  build: transmission
  volumes_from:
    - downloads
  ports:
    - "9091:9091"
